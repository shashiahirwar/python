t=(1,2,2,2,2,2,2,2)
print(2 in t)
print(len(t))
# t.append(3) --- IGNORE ---
# print(t) --- IGNORE ---
# t.remove(2) --- IGNORE ---
# print(t) --- IGNORE ---
# t --- IGNORE ---
# print(sizeof(t)) --- IGNORE ---
# print(t[1]) --- IGNORE ---
print(t.count(2))